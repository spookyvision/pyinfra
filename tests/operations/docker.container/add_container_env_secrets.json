{
    "kwargs": {
        "container": "nginx",
        "image": "nginx:alpine",
        "secrets": [
            "env_secret,type=env",
            "env_with_target,type=env,target=CUST_ENV_SCRT"
        ],
        "present": "true",
        "start": false
    },
    "facts": {
        "docker.DockerContainer": {
            "object_id=nginx": []
        }
    },
    "commands": [
        "docker container create --name nginx --secret env_secret,type=env --secret env_with_target,type=env,target=CUST_ENV_SCRT nginx:alpine"
    ]
}